type Post {
  id: ID!
  title: String!
  content: String!
  author: Author! @relation(name: "PostAuthor")
}

type Author {
  id: ID!
  name: String!
  imageUrl: String!
  posts: [Post] @relation(name: "PostAuthor")
}